class Target {
  int record;
  int consecutiveDays;
  int dayTarget;
  int learnedWords;
  int newWords;
  int time;

  Target(this.record, this.consecutiveDays, this.dayTarget, this.learnedWords, this.newWords, this.time);
}
