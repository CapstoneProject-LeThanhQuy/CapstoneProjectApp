class Course {
  int id;
  String title;
  String image;
  int totalWords;
  int learnedWords;
  int member;
  int progress;

  Course(this.id, this.title, this.image, this.totalWords, this.learnedWords, this.member, this.progress);
}
